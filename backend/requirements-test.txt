# Test-only dependencies. Not required to run the app itself — only to
# run the test suite in backend/tests/. Install with:
#   pip install -r requirements.txt -r requirements-test.txt
pytest
pytest-asyncio
httpx
mongomock_motor
